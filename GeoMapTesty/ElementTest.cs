using System;
using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace GeoMapTesty
{
    [TestClass]
    public class MapElementTest
    {
        [TestMethod]
        public void test_czy_nagłówek()
        {
            //var element = new MapElement(code: 1);
            //element.AddA1("");
            //element.AddA2("");
            //element.AddKR("");
            //element.AddAttribute(new AtrybutOpisowy("A1", ""));
            //element.AddOpis(new Opis());
            //element.AddPunkt(new Punkt(x, y));
            //element.CountAttr = 1
            //element.CountOpis = 1
            //element.CountPunkt = 1
        }
    }
}
